```java
public class HelloWorld{
	public static void main(String[] args){
		System.out.println("HelloWorld");
	}
}
```

- 编译：cmd下切换到对于HelloWorld.java下的路径，输入`javac HelloWorld.java`，此时对应路径下多了`.class`的字节码文件。
- 运行：输入`java HelloWorld`进行运行